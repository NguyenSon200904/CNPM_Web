package com.example.warehouse.service;

import org.springframework.stereotype.Service;

@Service
public class InventoryService {
  // Hiện tại không cần logic vì không có bảng inventory
  // Nếu cần thêm logic khác cho trang tồn kho, bạn có thể thêm vào đây
}